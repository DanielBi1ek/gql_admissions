[pytest]
markers =
    integration: tests that run against live federation/gateway
filterwarnings =
    ignore:Passing a class to strawberry.scalar\(\) is deprecated.*:DeprecationWarning
    ignore:Event driven styled extensions for on_request_start or on_request_end are deprecated.*:DeprecationWarning
